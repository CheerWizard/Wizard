package application.graphics.uniform

import application.core.math.Vector4f

class UVector4f(
    var name: String,
    x: Float = 0f,
    y: Float = 0f,
    z: Float = 0f,
    w: Float = 0f
) : Vector4f(
    x = x,
    y = y,
    z = z,
    w = w
)